name: Checker Script CI

# Trigger the workflow on every pull request to the main or master branch
on:
  pull_request:
    branches:
      - main
      - master

jobs:
  run-checker:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code from the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Rust environment (required for cargo stylus commands)
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      # Step 3: Give execute permission to the checker.sh script
      - name: Make checker.sh executable
        run: chmod +x ./checker.sh

      # Step 4: Run the checker.sh script (it will handle fetching the latest SDK version)
      - name: Run checker script
        run: ./checker.sh

      # Optionally: Check the output log for any failed tests (extra safety check)
      - name: Check for issues in the log
        if: success()
        run: |
          if grep -q "FAILED" check_results.log; then
            echo "Checker script failed! Fix the issues before merging."
            exit 1
          else
            echo "Checker script passed! Ready to merge."
          fi
